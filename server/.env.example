# Used by local dev (`npm run dev`). When running via `docker compose`,
# DATABASE_URL is overridden to point at the internal `db` service.
DATABASE_URL=postgresql://postgres:root@localhost:2345/straif
VERSION=dev
STEAM_API_KEY=<your steam api key>
DISCORD_TOKEN=<discord bot token>
PORT=3000
